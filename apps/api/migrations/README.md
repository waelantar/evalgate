# Database migrations

The migration environment is bootstrapped, but the first schema revision belongs to EG-003. Migration files must contain literal reviewed schema decisions; they must never derive pgvector width from runtime configuration.
